export interface Murale {
    id: number;
    name: string;
    coords: [number, number];
    details: string;
  }
  
  export interface Sponsor {
    id: number;
    name: string;
    coords: [number, number];
    details: string;
  }